name: zyra-test

"on":
  workflow_dispatch:
  push:
    branches: ["**"]

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Prueba de vida
        run: echo "Zyra Network CI funciona"
