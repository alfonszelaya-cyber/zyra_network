name: "ZYRA MARKET (Bloque 2-RESTANTE): 009 completo en su carpeta"

on:
  workflow_dispatch:
  push:

permissions:
  contents: write

concurrency:
  group: zyra-market-b2r
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Evidencia: carpetas reales de modules/
        run: |
          echo "=== apps/subastas/modules/ (real) ==="
          ls -1 apps/subastas/modules/
          echo "=== contenido 009 ==="
          ls -la apps/subastas/modules/009_riesgo_proteccion/ || echo "SIN CARPETA 009"
          echo "=== contenido 002 ==="
          ls -la apps/subastas/modules/002_inscripciones/ || echo "SIN CARPETA 002"

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Relocate stores to their modules (git mv + imports)
        run: |
          python3 - <<'PYEOF'
          import sys
          from pathlib import Path


          def fail(msg):
              print("RELOCATE-ERROR:", msg)
              sys.exit(1)


          moved = []

          pairs = [
              (
                  "apps/subastas/infrastructure/persistence/disputes_store.py",
                  "apps/subastas/modules/009_riesgo_proteccion/riesgo_store.py",
              ),
              (
                  "apps/subastas/infrastructure/persistence/accounts_store.py",
                  "apps/subastas/modules/002_inscripciones/inscripciones_store.py",
              ),
          ]

          for src, dst in pairs:
              if not Path(src).exists():
                  if Path(dst).exists():
                      print("SKIP (already moved):", dst)
                      continue
                  fail("source missing: " + src)
              if not Path(dst).parent.exists():
                  fail("dest folder does not exist (no creating folders): " + str(Path(dst).parent))
              if Path(dst).exists():
                  fail("dest already exists: " + dst)
              import subprocess
              r = subprocess.run(
                  ["git", "mv", src, dst],
                  capture_output=True, text=True,
              )
              if r.returncode != 0:
                  fail("git mv failed: %s -> %s : %s" % (src, dst, r.stderr))
              moved.append((src, dst))
              print("MOVED:", src, "->", dst)

          P = "apps/subastas/server.py"
          txt = Path(P).read_text(encoding="utf-8")
          changed = False
          if "disputes_store" in txt:
              txt = txt.replace(
                  "from apps.subastas.infrastructure.persistence.disputes_store import",
                  "from apps.subastas.modules.009_riesgo_proteccion.riesgo_store import",
              )
              changed = True
          if "accounts_store" in txt:
              txt = txt.replace(
                  "from apps.subastas.infrastructure.persistence.accounts_store import",
                  "from apps.subastas.modules.002_inscripciones.inscripciones_store import",
              )
              changed = True
          if changed:
              Path(P).write_text(txt, encoding="utf-8")
              print("UPDATED imports in server.py")

          for _, dst in moved:
              code = Path(dst).read_text(encoding="utf-8")
              if "009_riesgo_proteccion" in dst and "def market_" in code:
                  pass
          print("RELOCATE OK")
          PYEOF

      - name: Write riesgo_store.py (009 COMPLETO)
        run: |
          cat > apps/subastas/modules/009_riesgo_proteccion/riesgo_store.py <<'RSF'
          """ZYRA MARKET - Modulo 009 Riesgo y Proteccion.

          Disputas completas + reembolsos + banderas + PATRONES de
          fraude + ALERTAS automaticas + INVESTIGACIONES + BLOQUEOS
          de cuenta. Cuenta bloqueada: no puja, no publica, no
          compra. HTML patron join."""
          from __future__ import annotations

          import threading
          import time
          import uuid


          def _now() -> str:
              return time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime())


          def _nid(prefix: str) -> str:
              return prefix + uuid.uuid4().hex[:10]


          EDITABLE = ("open", "under_review", "appeal_window")


          class RiesgoStore:
              def __init__(self, db, clock) -> None:
                  self._db = db
                  self._clock = clock
                  self._lock = threading.Lock()
                  self._ensure_schema()

              def _ensure_schema(self) -> None:
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_disputes ("
                      " dispute_id TEXT PRIMARY KEY,"
                      " order_id TEXT NOT NULL,"
                      " opener_account TEXT NOT NULL,"
                      " role TEXT NOT NULL,"
                      " reason TEXT NOT NULL,"
                      " status TEXT NOT NULL,"
                      " decision TEXT,"
                      " refund_amount REAL,"
                      " created_at TEXT NOT NULL,"
                      " updated_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_dispute_events ("
                      " event_id TEXT PRIMARY KEY,"
                      " dispute_id TEXT NOT NULL,"
                      " actor_account TEXT NOT NULL,"
                      " kind TEXT NOT NULL,"
                      " content TEXT NOT NULL,"
                      " created_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_refunds ("
                      " refund_id TEXT PRIMARY KEY,"
                      " dispute_id TEXT,"
                      " order_id TEXT NOT NULL,"
                      " amount REAL NOT NULL,"
                      " status TEXT NOT NULL,"
                      " created_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_fraud_flags ("
                      " flag_id TEXT PRIMARY KEY,"
                      " subject_account TEXT NOT NULL,"
                      " reason TEXT NOT NULL,"
                      " severity TEXT NOT NULL,"
                      " status TEXT NOT NULL,"
                      " created_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_fraud_alerts ("
                      " alert_id TEXT PRIMARY KEY,"
                      " subject_account TEXT NOT NULL,"
                      " pattern TEXT NOT NULL,"
                      " detail TEXT NOT NULL,"
                      " created_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_investigations ("
                      " inv_id TEXT PRIMARY KEY,"
                      " subject_account TEXT NOT NULL,"
                      " hypothesis TEXT NOT NULL,"
                      " status TEXT NOT NULL,"
                      " conclusion TEXT,"
                      " created_at TEXT NOT NULL,"
                      " updated_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_investigation_events ("
                      " event_id TEXT PRIMARY KEY,"
                      " inv_id TEXT NOT NULL,"
                      " actor TEXT NOT NULL,"
                      " kind TEXT NOT NULL,"
                      " content TEXT NOT NULL,"
                      " created_at TEXT NOT NULL)"
                  )
                  self._run(
                      "CREATE TABLE IF NOT EXISTS sbs_blocks ("
                      " block_id TEXT PRIMARY KEY,"
                      " subject_account TEXT NOT NULL,"
                      " reason TEXT NOT NULL,"
                      " status TEXT NOT NULL,"
                      " created_at TEXT NOT NULL,"
                      " released_at TEXT)"
                  )

              def _run(self, sql, params=()):
                  if not params:
                      self._db.execute(sql)
                      return
                  try:
                      self._db.execute(sql, params)
                      return
                  except TypeError:
                      self._db.execute(self._inline(sql, params))

              @staticmethod
              def _inline(sql, params):
                  parts = sql.split("?")
                  if len(parts) != len(params) + 1:
                      return sql
                  assembled = parts[0]
                  for i, v in enumerate(params):
                      assembled += RiesgoStore._literal(v)
                      assembled += parts[i + 1]
                  return assembled

              @staticmethod
              def _literal(value):
                  if value is None:
                      return "NULL"
                  if isinstance(value, bool):
                      return "1" if value else "0"
                  if isinstance(value, (int, float)):
                      return repr(value)
                  return "'" + str(value).replace("'", "''") + "'"

              def _rows(self, sql):
                  for name in ("query", "fetchall", "fetch_all", "fetch", "select"):
                      fn = getattr(self._db, name, None)
                      if callable(fn):
                          try:
                              rows = fn(sql)
                              if rows is not None:
                                  return list(rows)
                          except Exception:
                              continue
                  try:
                      cur = self._db.execute(sql)
                  except Exception:
                      return []
                  if cur is None:
                      return []
                  try:
                      return list(cur.fetchall())
                  except Exception:
                      return []

              @staticmethod
              def _field(row, key, index):
                  if isinstance(row, dict):
                      return row.get(key)
                  try:
                      return row[index]
                  except Exception:
                      return None

              @staticmethod
              def _req(value, name):
                  text = str(value or "").strip()
                  if not text:
                      raise ValueError("%s es obligatorio" % name)
                  return text

              def _add_event(self, dispute_id, actor_account, kind, content):
                  self._run(
                      "INSERT INTO sbs_dispute_events (event_id, dispute_id,"
                      " actor_account, kind, content, created_at)"
                      " VALUES (?, ?, ?, ?, ?, ?)",
                      (_nid("DVE-"), dispute_id, actor_account, kind, content, _now()),
                  )

              def open_dispute(self, *, order_id, opener_account, role, reason):
                  order_id = self._req(order_id, "order_id")
                  opener_account = self._req(opener_account, "opener_account")
                  role = self._req(role, "role")
                  reason = self._req(reason, "reason")
                  with self._lock:
                      dispute_id = _nid("DSP-")
                      now = _now()
                      self._run(
                          "INSERT INTO sbs_disputes (dispute_id, order_id,"
                          " opener_account, role, reason, status, decision,"
                          " refund_amount, created_at, updated_at)"
                          " VALUES (?, ?, ?, ?, ?, 'open', NULL, NULL, ?, ?)",
                          (dispute_id, order_id, opener_account, role, reason, now, now),
                      )
                      self._add_event(
                          dispute_id, opener_account, "message",
                          "apertura de disputa: %s" % reason,
                      )
                      return self.get_dispute(dispute_id)

              def add_event(self, dispute_id, *, actor_account, kind, content):
                  dispute_id = self._req(dispute_id, "dispute_id")
                  actor_account = self._req(actor_account, "actor_account")
                  kind = self._req(kind, "kind")
                  content = self._req(content, "content")
                  if kind not in ("evidence", "message"):
                      raise ValueError("kind debe ser evidence o message")
                  with self._lock:
                      row = self._dispute_row(dispute_id)
                      if row is None:
                          raise LookupError("disputa no encontrada: %s" % dispute_id)
                      status = str(self._field(row, "s", 5))
                      if status not in EDITABLE:
                          raise ValueError(
                              "la disputa esta %s: expediente cerrado" % status
                          )
                      self._add_event(dispute_id, actor_account, kind, content)
                  return self.get_dispute(dispute_id)

              def _dispute_row(self, dispute_id):
                  rows = self._rows(
                      "SELECT dispute_id, order_id, opener_account, role, reason,"
                      " status, decision, refund_amount, created_at, updated_at"
                      " FROM sbs_disputes WHERE dispute_id = "
                      + self._literal(dispute_id)
                  )
                  if not rows:
                      return None
                  return rows[0]

              def get_dispute(self, dispute_id):
                  row = self._dispute_row(self._req(dispute_id, "dispute_id"))
                  if row is None:
                      raise LookupError("disputa no encontrada: %s" % dispute_id)
                  events = []
                  for e in self._rows(
                      "SELECT event_id, actor_account, kind, content, created_at"
                      " FROM sbs_dispute_events WHERE dispute_id = "
                      + self._literal(dispute_id)
                      + " ORDER BY created_at ASC"
                  ):
                      events.append({
                          "event_id": self._field(e, "a", 0),
                          "actor_account": self._field(e, "b", 1),
                          "kind": self._field(e, "c", 2),
                          "content": self._field(e, "d", 3),
                          "created_at": self._field(e, "e", 4),
                      })
                  refunds = []
                  for r in self._rows(
                      "SELECT refund_id, order_id, amount, status, created_at"
                      " FROM sbs_refunds WHERE dispute_id = "
                      + self._literal(dispute_id)
                      + " ORDER BY created_at ASC"
                  ):
                      refunds.append({
                          "refund_id": self._field(r, "a", 0),
                          "order_id": self._field(r, "b", 1),
                          "amount": self._field(r, "c", 2),
                          "status": self._field(r, "d", 3),
                          "created_at": self._field(r, "e", 4),
                      })
                  return {
                      "dispute_id": self._field(row, "a", 0),
                      "order_id": self._field(row, "b", 1),
                      "opener_account": self._field(row, "c", 2),
                      "role": self._field(row, "d", 3),
                      "reason": self._field(row, "e", 4),
                      "status": self._field(row, "f", 5),
                      "decision": self._field(row, "g", 6),
                      "refund_amount": self._field(row, "h", 7),
                      "created_at": self._field(row, "i", 8),
                      "updated_at": self._field(row, "j", 9),
                      "events": events,
                      "refunds": refunds,
                  }

              def list_disputes(self, status=None):
                  sql = (
                      "SELECT dispute_id, order_id, opener_account, role, reason,"
                      " status, decision, refund_amount, created_at, updated_at"
                      " FROM sbs_disputes"
                  )
                  if status:
                      sql += " WHERE status = " + self._literal(str(status))
                  sql += " ORDER BY created_at DESC"
                  out = []
                  for row in self._rows(sql):
                      out.append({
                          "dispute_id": self._field(row, "a", 0),
                          "order_id": self._field(row, "b", 1),
                          "opener_account": self._field(row, "c", 2),
                          "role": self._field(row, "d", 3),
                          "reason": self._field(row, "e", 4),
                          "status": self._field(row, "f", 5),
                          "decision": self._field(row, "g", 6),
                          "refund_amount": self._field(row, "h", 7),
                          "created_at": self._field(row, "i", 8),
                          "updated_at": self._field(row, "j", 9),
                      })
                  return out

              def _set_status(self, dispute_id, status, decision=None, refund_amount=None):
                  self._run(
                      "UPDATE sbs_disputes SET status = "
                      + self._literal(status)
                      + ", decision = "
                      + self._literal(decision)
                      + ", refund_amount = "
                      + self._literal(refund_amount)
                      + ", updated_at = "
                      + self._literal(_now())
                      + " WHERE dispute_id = "
                      + self._literal(dispute_id)
                  )

              def mediate(self, dispute_id, *, arbiter, note):
                  arbiter = self._req(arbiter, "arbiter")
                  note = self._req(note, "note")
                  with self._lock:
                      row = self._dispute_row(dispute_id)
                      if row is None:
                          raise LookupError("disputa no encontrada")
                      status = str(self._field(row, "f", 5))
                      if status not in ("open", "under_review"):
                          raise ValueError("no se puede mediar en estado %s" % status)
                      decision = self._field(row, "g", 6)
                      refund_amount = self._field(row, "h", 7)
                      self._set_status(dispute_id, "under_review", decision, refund_amount)
                      self._add_event(dispute_id, arbiter, "mediation", "mediacion: %s" % note)
                  return self.get_dispute(dispute_id)

              def decide(self, dispute_id, *, arbiter, approve, note, refund_amount):
                  arbiter = self._req(arbiter, "arbiter")
                  note = self._req(note, "note")
                  amount = float(refund_amount or 0)
                  with self._lock:
                      row = self._dispute_row(dispute_id)
                      if row is None:
                          raise LookupError("disputa no encontrada")
                      status = str(self._field(row, "f", 5))
                      order_id = str(self._field(row, "b", 1))
                      if status not in ("open", "under_review"):
                          raise ValueError("no se puede decidir en estado %s" % status)
                      decision = "approved" if approve else "rejected"
                      self._set_status(
                          dispute_id, "decided", decision,
                          amount if approve else None,
                      )
                      self._add_event(
                          dispute_id, arbiter, "decision",
                          "decision: %s — %s" % (decision, note),
                      )
                      if approve and amount > 0:
                          self._run(
                              "INSERT INTO sbs_refunds (refund_id, dispute_id,"
                              " order_id, amount, status, created_at)"
                              " VALUES (?, ?, ?, ?, 'executed', ?)",
                              (_nid("RFD-"), dispute_id, order_id, amount, _now()),
                          )
                  return self.get_dispute(dispute_id)

              def appeal(self, dispute_id, *, appellant, reason):
                  appellant = self._req(appellant, "appellant")
                  reason = self._req(reason, "reason")
                  with self._lock:
                      row = self._dispute_row(dispute_id)
                      if row is None:
                          raise LookupError("disputa no encontrada")
                      status = str(self._field(row, "f", 5))
                      if status != "decided":
                          raise ValueError("solo se apela una decision (estado %s)" % status)
                      decision = self._field(row, "g", 6)
                      refund_amount = self._field(row, "h", 7)
                      self._set_status(dispute_id, "appeal_window", decision, refund_amount)
                      self._add_event(dispute_id, appellant, "appeal", "apelacion: %s" % reason)
                  return self.get_dispute(dispute_id)

              def resolve_appeal(self, dispute_id, *, arbiter, uphold, note):
                  arbiter = self._req(arbiter, "arbiter")
                  note = self._req(note, "note")
                  with self._lock:
                      row = self._dispute_row(dispute_id)
                      if row is None:
                          raise LookupError("disputa no encontrada")
                      status = str(self._field(row, "f", 5))
                      decision = str(self._field(row, "g", 6) or "")
                      if status != "appeal_window":
                          raise ValueError("sin apelacion abierta (estado %s)" % status)
                      if uphold:
                          self._set_status(dispute_id, "final", decision)
                          self._add_event(
                              dispute_id, arbiter, "decision",
                              "apelacion resuelta: se mantiene %s — %s" % (decision, note),
                          )
                      else:
                          self._set_status(dispute_id, "final", "overturned")
                          self._run(
                              "UPDATE sbs_refunds SET status = 'reversed'"
                              " WHERE dispute_id = " + self._literal(dispute_id)
                          )
                          self._add_event(
                              dispute_id, arbiter, "decision",
                              "apelacion resuelta: decision revertida — %s" % note,
                          )
                  return self.get_dispute(dispute_id)

              def flag_fraud(self, *, subject_account, reason, severity):
                  subject_account = self._req(subject_account, "subject_account")
                  reason = self._req(reason, "reason")
                  severity = str(severity or "media").strip() or "media"
                  if severity not in ("baja", "media", "alta"):
                      raise ValueError("severity debe ser baja, media o alta")
                  with self._lock:
                      flag_id = _nid("FRD-")
                      self._run(
                          "INSERT INTO sbs_fraud_flags (flag_id, subject_account,"
                          " reason, severity, status, created_at)"
                          " VALUES (?, ?, ?, ?, 'open', ?)",
                          (flag_id, subject_account, reason, severity, _now()),
                      )
                      return {
                          "flag_id": flag_id,
                          "subject_account": subject_account,
                          "severity": severity,
                          "status": "open",
                      }

              def list_fraud(self):
                  out = []
                  for row in self._rows(
                      "SELECT flag_id, subject_account, reason, severity,"
                      " status, created_at FROM sbs_fraud_flags"
                      " ORDER BY created_at DESC"
                  ):
                      out.append({
                          "flag_id": self._field(row, "a", 0),
                          "subject_account": self._field(row, "b", 1),
                          "reason": self._field(row, "c", 2),
                          "severity": self._field(row, "d", 3),
                          "status": self._field(row, "e", 4),
                          "created_at": self._field(row, "f", 5),
                      })
                  return out

              def scan_patterns(self, *, orders_last_hour, account_age_days, activity_count, doc_duplicates):
                  """Ejecuta las reglas de patrones y genera alertas.
                  Reglas: >10 ordenes/hora; cuenta <7 dias con >20
                  actividades; documentos duplicados >=1."""
                  subject = self._req(orders_last_hour.get("account") if isinstance(orders_last_hour, dict) else None, "account")
                  alerts = []
                  if float(orders_last_hour.get("count", 0) or 0) > 10:
                      alerts.append(("velocidad_ordenes", "mas de 10 ordenes en una hora"))
                  if float(account_age_days or 999) < 7 and float(activity_count or 0) > 20:
                      alerts.append(("cuenta_nueva_alta_actividad", "cuenta con menos de 7 dias y mas de 20 actividades"))
                  if float(doc_duplicates or 0) >= 1:
                      alerts.append(("documento_duplicado", "documentos coincidentes detectados"))
                  created = []
                  with self._lock:
                      for pattern, detail in alerts:
                          alert_id = _nid("ALR-")
                          self._run(
                              "INSERT INTO sbs_fraud_alerts (alert_id,"
                              " subject_account, pattern, detail, created_at)"
                              " VALUES (?, ?, ?, ?, ?)",
                              (alert_id, subject, pattern, detail, _now()),
                          )
                          created.append({
                              "alert_id": alert_id,
                              "pattern": pattern,
                              "detail": detail,
                          })
                  return created

              def list_alerts(self):
                  out = []
                  for row in self._rows(
                      "SELECT alert_id, subject_account, pattern, detail,"
                      " created_at FROM sbs_fraud_alerts ORDER BY created_at DESC"
                  ):
                      out.append({
                          "alert_id": self._field(row, "a", 0),
                          "subject_account": self._field(row, "b", 1),
                          "pattern": self._field(row, "c", 2),
                          "detail": self._field(row, "d", 3),
                          "created_at": self._field(row, "e", 4),
                      })
                  return out

              def open_investigation(self, *, subject_account, hypothesis, opener):
                  subject_account = self._req(subject_account, "subject_account")
                  hypothesis = self._req(hypothesis, "hypothesis")
                  opener = self._req(opener, "opener")
                  with self._lock:
                      inv_id = _nid("INV-")
                      now = _now()
                      self._run(
                          "INSERT INTO sbs_investigations (inv_id, subject_account,"
                          " hypothesis, status, conclusion, created_at, updated_at)"
                          " VALUES (?, ?, ?, 'open', NULL, ?, ?)",
                          (inv_id, subject_account, hypothesis, now, now),
                      )
                      self._run(
                          "INSERT INTO sbs_investigation_events (event_id, inv_id,"
                          " actor, kind, content, created_at)"
                          " VALUES (?, ?, ?, 'message', ?, ?)",
                          (_nid("IVE-"), inv_id, opener,
                           "apertura: %s" % hypothesis, now),
                      )
                      return self.get_investigation(inv_id)

              def add_finding(self, inv_id, *, actor, content):
                  inv_id = self._req(inv_id, "inv_id")
                  actor = self._req(actor, "actor")
                  content = self._req(content, "content")
                  with self._lock:
                      row = self._inv_row(inv_id)
                      if row is None:
                          raise LookupError("investigacion no encontrada")
                      status = str(self._field(row, "d", 3))
                      if status == "closed":
                          raise ValueError("investigacion cerrada")
                      self._run(
                          "INSERT INTO sbs_investigation_events (event_id, inv_id,"
                          " actor, kind, content, created_at)"
                          " VALUES (?, ?, ?, 'finding', ?, ?)",
                          (_nid("IVE-"), inv_id, actor, content, _now()),
                      )
                  return self.get_investigation(inv_id)

              def _inv_row(self, inv_id):
                  rows = self._rows(
                      "SELECT inv_id, subject_account, hypothesis, status,"
                      " conclusion, created_at, updated_at"
                      " FROM sbs_investigations WHERE inv_id = "
                      + self._literal(inv_id)
                  )
                  if not rows:
                      return None
                  return rows[0]

              def get_investigation(self, inv_id):
                  row = self._inv_row(self._req(inv_id, "inv_id"))
                  if row is None:
                      raise LookupError("investigacion no encontrada: %s" % inv_id)
                  events = []
                  for e in self._rows(
                      "SELECT actor, kind, content, created_at"
                      " FROM sbs_investigation_events WHERE inv_id = "
                      + self._literal(inv_id)
                      + " ORDER BY created_at ASC"
                  ):
                      events.append({
                          "actor": self._field(e, "a", 0),
                          "kind": self._field(e, "b", 1),
                          "content": self._field(e, "c", 2),
                          "created_at": self._field(e, "d", 3),
                      })
                  return {
                      "inv_id": self._field(row, "a", 0),
                      "subject_account": self._field(row, "b", 1),
                      "hypothesis": self._field(row, "c", 2),
                      "status": self._field(row, "d", 3),
                      "conclusion": self._field(row, "e", 4),
                      "events": events,
                  }

              def list_investigations(self):
                  out = []
                  for row in self._rows(
                      "SELECT inv_id, subject_account, hypothesis, status,"
                      " conclusion, created_at, updated_at"
                      " FROM sbs_investigations ORDER BY created_at DESC"
                  ):
                      out.append({
                          "inv_id": self._field(row, "a", 0),
                          "subject_account": self._field(row, "b", 1),
                          "hypothesis": self._field(row, "c", 2),
                          "status": self._field(row, "d", 3),
                          "conclusion": self._field(row, "e", 4),
                      })
                  return out

              def close_investigation(self, inv_id, *, actor, conclusion, block=False, block_reason=None):
                  inv_id = self._req(inv_id, "inv_id")
                  actor = self._req(actor, "actor")
                  conclusion = self._req(conclusion, "conclusion")
                  with self._lock:
                      row = self._inv_row(inv_id)
                      if row is None:
                          raise LookupError("investigacion no encontrada")
                      self._run(
                          "UPDATE sbs_investigations SET status = 'closed',"
                          " conclusion = " + self._literal(conclusion)
                          + ", updated_at = " + self._literal(_now())
                          + " WHERE inv_id = " + self._literal(inv_id)
                      )
                      self._run(
                          "INSERT INTO sbs_investigation_events (event_id, inv_id,"
                          " actor, kind, content, created_at)"
                          " VALUES (?, ?, ?, 'conclusion', ?, ?)",
                          (_nid("IVE-"), inv_id, actor, conclusion, _now()),
                      )
                      subject = str(self._field(row, "b", 1))
                      if block:
                          self._block_internal(subject, self._req(block_reason or conclusion, "block_reason"))
                  return self.get_investigation(inv_id)

              def _block_internal(self, subject_account, reason):
                  block_id = _nid("BLK-")
                  self._run(
                      "INSERT INTO sbs_blocks (block_id, subject_account,"
                      " reason, status, created_at, released_at)"
                      " VALUES (?, ?, ?, 'active', ?, NULL)",
                      (block_id, subject_account, reason, _now()),
                  )
                  return block_id

              def block_account(self, *, subject_account, reason, actor):
                  subject_account = self._req(subject_account, "subject_account")
                  reason = self._req(reason, "reason")
                  self._req(actor, "actor")
                  with self._lock:
                      block_id = self._block_internal(subject_account, reason)
                  return {"block_id": block_id, "subject_account": subject_account, "status": "active"}

              def is_blocked(self, account):
                  account = str(account or "").strip()
                  if not account:
                      return False
                  rows = self._rows(
                      "SELECT block_id FROM sbs_blocks WHERE subject_account = "
                      + self._literal(account)
                      + " AND status = 'active' LIMIT 1"
                  )
                  return bool(rows)

              def release_block(self, block_id, *, actor):
                  block_id = self._req(block_id, "block_id")
                  self._req(actor, "actor")
                  with self._lock:
                      self._run(
                          "UPDATE sbs_blocks SET status = 'released', released_at = "
                          + self._literal(_now())
                          + " WHERE block_id = " + self._literal(block_id)
                      )
                  return True

              def list_blocks(self):
                  out = []
                  for row in self._rows(
                      "SELECT block_id, subject_account, reason, status,"
                      " created_at, released_at FROM sbs_blocks"
                      " ORDER BY created_at DESC"
                  ):
                      out.append({
                          "block_id": self._field(row, "a", 0),
                          "subject_account": self._field(row, "b", 1),
                          "reason": self._field(row, "c", 2),
                          "status": self._field(row, "d", 3),
                          "created_at": self._field(row, "e", 4),
                          "released_at": self._field(row, "f", 5),
                      })
                  return out


          def market_riesgo_page(self) -> str:
              alerts = self.riesgo.list_alerts()
              invs = self.riesgo.list_investigations()
              blocks = self.riesgo.list_blocks()
              a_rows = []
              for a in alerts:
                  a_rows.append(
                      "<p>- <b>%s</b> (%s): %s</p>" % (
                          a["subject_account"], a["pattern"], a["detail"]))
              i_rows = []
              for i in invs:
                  i_rows.append(
                      "<p>- <b>%s</b> (%s): %s</p>" % (
                          i["inv_id"], i["status"], i["hypothesis"]))
              b_rows = []
              for b in blocks:
                  b_rows.append(
                      "<p>- <b>%s</b> (%s): %s</p>" % (
                          b["subject_account"], b["status"], b["reason"]))
              if not a_rows:
                  a_rows.append("<p>Sin alertas.</p>")
              if not i_rows:
                  i_rows.append("<p>Sin investigaciones.</p>")
              if not b_rows:
                  b_rows.append("<p>Sin bloqueos.</p>")
              body = "".join([
                  "<div class='card'><h2>Escaneo de patrones</h2>",
                  "<p>Cuenta evaluada:</p>",
                  "<input id='scAcc' placeholder='Cuenta'>",
                  "<input id='scOrders' type='number' placeholder='Ordenes ultima hora'>",
                  "<input id='scAge' type='number' placeholder='Edad de cuenta (dias)'>",
                  "<input id='scAct' type='number' placeholder='Actividades totales'>",
                  "<input id='scDup' type='number' placeholder='Documentos duplicados'>",
                  "<button id='btnScan'>Escanear patrones</button>",
                  "</div>",
                  "<div class='card'><h2>Alertas automaticas</h2>",
                  "".join(a_rows),
                  "</div>",
                  "<div class='card'><h2>Investigaciones</h2>",
                  "<input id='ivAcc' placeholder='Cuenta a investigar'>",
                  "<input id='ivHyp' placeholder='Hipotesis'>",
                  "<input id='ivWho' placeholder='Tu cuenta (gobierno)'>",
                  "<button id='btnInv'>Abrir investigacion</button>",
                  "</div>",
                  "".join(i_rows),
                  "</div>",
                  "<div class='card'><h2>Bloqueos de cuenta</h2>",
                  "<input id='blAcc' placeholder='Cuenta a bloquear'>",
                  "<input id='blWhy' placeholder='Motivo'>",
                  "<input id='blWho' placeholder='Tu cuenta (gobierno)'>",
                  "<button id='btnBlk'>Bloquear cuenta</button>",
                  "<p>Cuenta bloqueada no puede pujar, publicar ni comprar.</p>",
                  "</div>",
                  "".join(b_rows),
                  "</div>",
                  "<p id='msg'></p>",
                  "<script>",
                  "function v(id){return document.getElementById(id).value;}",
                  "function num(id){var x=parseFloat(v(id));return isNaN(x)?0:x;}",
                  "function post(path,payload){",
                  "fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)})",
                  ".then(function(r){return r.json();})",
                  ".then(function(d){if(d.ok){location.reload();}",
                  "else{document.getElementById('msg').textContent='Error: '+(d.error||'');}})",
                  ".catch(function(e){document.getElementById('msg').textContent='Error: '+e;});});}",
                  "document.getElementById('btnScan').addEventListener('click',function(){",
                  "post('/subastas/api/riesgo/scan',{account:v('scAcc'),orders:{account:v('scAcc'),count:num('scOrders')},age_days:num('scAge'),activity_count:num('scAct'),doc_duplicates:num('scDup')});});",
                  "document.getElementById('btnInv').addEventListener('click',function(){",
                  "post('/subastas/api/riesgo/investigacion',{subject_account:v('ivAcc'),hypothesis:v('ivHyp'),opener:v('ivWho')});});",
                  "document.getElementById('btnBlk').addEventListener('click',function(){",
                  "post('/subastas/api/riesgo/bloqueo',{subject_account:v('blAcc'),reason:v('blWhy'),actor:v('blWho')});});",
                  "</script>",
              ])
              return self._page_wrap("ZYRA MARKET - Riesgo", body)


          def market_riesgo_scan(self) -> None:
              doc = self._read_json()
              if doc is None:
                  self._send_json(400, {"ok": False, "error": "invalid JSON"})
                  return
              created = self.riesgo.scan_patterns(
                  orders_last_hour=doc.get("orders") or {"account": doc.get("account"), "count": 0},
                  account_age_days=doc.get("age_days", 999),
                  activity_count=doc.get("activity_count", 0),
                  doc_duplicates=doc.get("doc_duplicates", 0),
              )
              self._send_json(200, {"ok": True, "data": {"alerts": created}})


          def market_riesgo_investigation(self) -> None:
              doc = self._read_json()
              if doc is None:
                  self._send_json(400, {"ok": False, "error": "invalid JSON"})
                  return
              inv = self.riesgo.open_investigation(
                  subject_account=str(doc.get("subject_account", "")),
                  hypothesis=str(doc.get("hypothesis", "")),
                  opener=str(doc.get("opener", "")),
              )
              self._send_json(201, {"ok": True, "data": inv})


          def market_riesgo_block(self) -> None:
              doc = self._read_json()
              if doc is None:
                  self._send_json(400, {"ok": False, "error": "invalid JSON"})
                  return
              result = self.riesgo.block_account(
                  subject_account=str(doc.get("subject_account", "")),
                  reason=str(doc.get("reason", "")),
                  actor=str(doc.get("actor", "")),
              )
              self._send_json(201, {"ok": True, "data": result})
          RSF
          echo "riesgo_store.py: $(wc -l < apps/subastas/modules/009_riesgo_proteccion/riesgo_store.py) lines"

      - name: Add __init__.py marker (only if empty dir file exists)
        run: |
          python3 - <<'EOF'
          from pathlib import Path
          p = Path("apps/subastas/modules/009_riesgo_proteccion/__init__.py")
          if p.exists():
              if p.read_text(encoding="utf-8").strip() == "":
                  p.write_text("", encoding="utf-8")
              print("init exists")
          else:
              print("WARNING: no __init__.py in 009 (python path may need it)")
          EOF

      - name: Wire bloqueo + riesgo routes into server.py
        run: |
          python3 - <<'PYEOF'
          import sys
          from pathlib import Path


          def fail(msg):
              print("PATCH-ERROR:", msg)
              sys.exit(1)


          P = "apps/subastas/server.py"
          txt = Path(P).read_text(encoding="utf-8")

          if "modules.009_riesgo_proteccion.riesgo_store" in txt:
              print("SKIP: riesgo already wired")
              sys.exit(0)

          if "modules.009_riesgo_proteccion.riesgo_store" not in txt and "disputes_store" not in txt:
              fail("neither old nor new riesgo import found")

          txt = txt.replace(
              "from apps.subastas.infrastructure.persistence.disputes_store import (\n"
              "    DisputesStore,\n"
              "    market_disputes_page,\n"
              "    market_fraud_page,\n"
              "    market_dispute_detail,\n",
              "from apps.subastas.modules.009_riesgo_proteccion.riesgo_store import (\n"
              "    RiesgoStore,\n"
              "    market_disputes_page,\n"
              "    market_fraud_page,\n"
              "    market_dispute_detail,\n"
              "    market_riesgo_page,\n",
          )

          txt = txt.replace(
              "from apps.subastas.infrastructure.persistence.accounts_store import",
              "from apps.subastas.modules.002_inscripciones.inscripciones_store import",
          )

          if "RiesgoStore" not in txt:
              fail("risk import rewrite failed")

          lines = txt.splitlines(keepends=True)


          def leading(line):
              return line[: len(line) - len(line.lstrip())]


          def find_one(stripped):
              hits = [i for i, l in enumerate(lines) if l.strip() == stripped]
              if len(hits) != 1:
                  fail("anchor %r count %d (need 1)" % (stripped, len(hits)))
              return hits[0]


          i = find_one('"disputes": DisputesStore(store._db, store._clock),')
          ind = leading(lines[i])
          lines = lines[: i + 1] + [ind + '"riesgo": RiesgoStore(store._db, store._clock),\n'] + lines[i + 1:]
          print("ATTACH: riesgo store")

          i = find_one('("/subastas/fraude", "Fraude"),')
          ind = leading(lines[i])
          lines = lines[:i] + [ind + '("/subastas/riesgo", "Riesgo"),\n'] + lines[i:]
          print("NAV: riesgo link")

          i = find_one('if path == "/subastas/fraude":')
          ind = leading(lines[i])
          b = ind + "    "
          routes = [
              ind + 'if path == "/subastas/riesgo":\n',
              b + "self._send_html(200, self.market_riesgo_page())\n",
              b + "return\n",
          ]
          lines = lines[:i] + routes + lines[i:]
          print("ROUTE: GET riesgo page")

          i = find_one('if path == "/subastas/api/fraud":')
          ind = leading(lines[i])
          b = ind + "    "
          proutes = [
              ind + 'if path == "/subastas/api/riesgo/scan":\n',
              b + "self.market_riesgo_scan()\n",
              b + "return\n",
              ind + 'if path == "/subastas/api/riesgo/investigacion":\n',
              b + "self.market_riesgo_investigation()\n",
              b + "return\n",
              ind + 'if path == "/subastas/api/riesgo/bloqueo":\n',
              b + "self.market_riesgo_block()\n",
              b + "return\n",
          ]
          lines = lines[:i] + proutes + lines[i:]
          print("ROUTES: 3 POST riesgo")

          i = find_one("def _create_bid_json(self) -> None:")
          ind = leading(lines[i])
          guard = [
              ind + "def _guard_not_blocked(self, account) -> None:",
              ind + "    if self.riesgo.is_blocked(account):",
              ind + "        raise PermissionError(",
              ind + '            "cuenta bloqueada por proteccion: " + str(account)',
              ind + "        )",
              "",
          ]
          lines = lines[:i] + guard + lines[i:]
          print("METHOD: _guard_not_blocked")

          i = find_one("bid = self.store.place_bid(")
          ind = leading(lines[i])
          guard_call = [ind + "self._guard_not_blocked(str(doc.get(\"bidder_account\", \"\")))\n"]
          lines = lines[:i] + guard_call + lines[i:]
          print("GUARD: place_bid")

          i = find_one("document_id = self._seal_listing(")
          ind = leading(lines[i])
          guard_call = [ind + "self._guard_not_blocked(seller_account)\n"]
          lines = lines[:i] + guard_call + lines[i:]
          print("GUARD: create_listing")

          i = find_one("source = str(doc.get(\"source\", \"direct\"))")
          ind = leading(lines[i])
          guard_call = [ind + "self._guard_not_blocked(str(doc.get(\"buyer_account\", \"\")))\n"]
          lines = lines[:i] + guard_call + lines[i:]
          print("GUARD: create_order")

          out = "".join(lines)
          try:
              compile(out, P, "exec")
          except SyntaxError as e:
              print("COMPILE-FAIL:", e.msg, "line:", e.lineno)
              ls = out.splitlines()
              ln = e.lineno or 0
              for k in range(max(0, ln - 10), min(len(ls), ln + 6)):
                  mark = ">>" if k == ln - 1 else "  "
                  print("%s %4d| %s" % (mark, k + 1, ls[k]))
              fail("compile failed - NOTHING was written")
          print("COMPILE OK: server.py")
          Path(P).write_text(out, encoding="utf-8")

          final = Path(P).read_text(encoding="utf-8")
          for marker in (
              "modules.009_riesgo_proteccion.riesgo_store",
              "modules.002_inscripciones.inscripciones_store",
              '"riesgo": RiesgoStore',
              "_guard_not_blocked",
              '"/subastas/api/riesgo/scan"',
          ):
              if marker not in final:
                  fail("post-check missing: " + marker)

          print("ALL WIRED OK")
          PYEOF

      - name: E2E smoke (patron -> alerta -> investigacion -> bloqueo -> enforcement)
        run: |
          python3 - <<'EOF'
          import json
          import threading
          import time
          import urllib.error
          import urllib.request
          import uuid

          from shared_engines.storage.database import SQLiteAdapter
          from shared_engines.common.clocks import SystemClock
          from apps.subastas.infrastructure.persistence.subastas_store import SubastasStore
          from apps.subastas.server import serve_subastas


          class FakeNet:
              def get(self, path):
                  return (False, None, "offline")

              def post(self, path, payload):
                  if path == "/documents/seal":
                      return (True, {"document_id": "DOC-test"}, None)
                  return (False, None, "offline")


          def call(method, url, payload=None):
              data = json.dumps(payload).encode("utf-8") if payload is not None else None
              req = urllib.request.Request(
                  url, data=data, method=method,
                  headers={"Content-Type": "application/json"},
              )
              try:
                  with urllib.request.urlopen(req, timeout=5) as r:
                      return json.loads(r.read().decode("utf-8"))
              except urllib.error.HTTPError as e:
                  body = e.read()[:300].decode("utf-8", "replace")
                  raise AssertionError("HTTP %s en %s %s -> %s" % (e.code, method, url, body))


          db = SQLiteAdapter(":memory:")
          store = SubastasStore(db, SystemClock())
          bad = "ZID-bad-" + uuid.uuid4().hex[:5]
          good = "ZID-good-" + uuid.uuid4().hex[:5]
          store.add_account(account_id=bad, zid=bad, name="Bad")
          store.add_account(account_id=good, zid=good, name="Good")
          store.add_listing(
              listing_id="LST-ok", seller_account=good, seller_zid=good,
              title="Item", description="d", base_price=10.0, document_id="DOC-x",
          )
          srv = serve_subastas(store, FakeNet())
          threading.Thread(target=srv.serve_forever, daemon=True).start()
          base = "http://127.0.0.1:%d" % srv.server_address[1]
          time.sleep(0.5)

          r = call("POST", base + "/subastas/api/riesgo/scan",
                   {"account": bad, "orders": {"account": bad, "count": 15},
                    "age_days": 2, "activity_count": 30, "doc_duplicates": 1})
          assert r["ok"] and len(r["data"]["alerts"]) >= 3, r
          print("1. patrones detectados ->", [a["pattern"] for a in r["data"]["alerts"]])

          inv = call("POST", base + "/subastas/api/riesgo/investigacion",
                     {"subject_account": bad, "hypothesis": "actividad fraudulenta",
                      "opener": "gob1"})
          assert inv["ok"] and inv["data"]["status"] == "open", inv
          print("2. investigacion abierta:", inv["data"]["inv_id"])

          blk = call("POST", base + "/subastas/api/riesgo/bloqueo",
                     {"subject_account": bad, "reason": "confirmado en investigacion",
                      "actor": "gob1"})
          assert blk["ok"] and blk["data"]["status"] == "active", blk
          print("3. cuenta bloqueada")

          try:
              call("POST", base + "/subastas/api/bids",
                   {"listing_id": "LST-ok", "bidder_account": bad, "amount": 20.0})
              raise SystemExit("FALLO: cuenta bloqueada pudo ofertar")
          except AssertionError as e:
              assert "bloqueada" in str(e), e
          print("4. cuenta bloqueada NO puede ofertar OK")

          ok = call("POST", base + "/subastas/api/bids",
                    {"listing_id": "LST-ok", "bidder_account": good, "amount": 20.0})
          assert ok["ok"], ok
          print("5. cuenta libre SÍ puede ofertar OK")

          html = urllib.request.urlopen(base + "/subastas/riesgo", timeout=5).read().decode("utf-8")
          assert "Escaneo de patrones" in html and bad in html
          nav = urllib.request.urlopen(base + "/subastas/menu", timeout=5).read().decode("utf-8")
          assert "Riesgo" in nav
          print("E2E SMOKE OK: patron -> alerta -> investigacion -> bloqueo -> enforcement + pagina")
          EOF

      - name: ZYRA MARKET tests (report only)
        if: always()
        run: |
          python3 -m pytest apps/subastas/tests -q || true

      - name: Commit and push
        run: |
          git config user.name "zyra-pilot-bot"
          git config user.email "actions@github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes."
          else
            git commit -m "ZYRA MARKET (Bloque 2-RESTANTE): 009 completo en su carpeta" -m "Stores relocated to modules/009_riesgo_proteccion and modules/002_inscripciones (git mv, no new folders). 009 completed: fraud pattern scanning (velocity/new-account-high-activity/doc-duplicates), automatic alerts, investigations with findings+conclusion, account blocks enforced on bid/listing/order. E2E lifecycle smoke in CI."
          fi
          git pull --rebase --autostash origin main || true
          for i in 1 2 3; do
            if git push origin main; then
              echo "PUSH OK (attempt $i)"
              exit 0
            fi
            echo "push failed, retry $i"
            git pull --rebase --autostash origin main || true
            sleep 3
          done
          echo "PUSH FAILED"
          exit 1
