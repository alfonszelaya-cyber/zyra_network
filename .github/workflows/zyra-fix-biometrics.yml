name: ZYRA All Apps Final

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Patch MPE and SEMILLA (kernel enrollment)
        run: |
          python3 - <<'PYEOF'
          from pathlib import Path

          def patch_file(target, anchor, addition, marker):
              path = Path(target)
              if not path.exists():
                  print(f"SKIP missing: {target}")
                  return
              src = path.read_text(encoding="utf-8")
              if marker in src:
                  print(f"ALREADY: {target}")
                  return
              if anchor not in src:
                  print(f"NO ANCHOR: {target}")
                  return
              src = src.replace(anchor, anchor + addition, 1)
              path.write_text(src, encoding="utf-8")
              print(f"PATCHED: {target}")
              import ast
              ast.parse(src)
              print(f"SYNTAX OK: {target}")

          MPE_ADD = (
              "        from apps.mi_primer_empleo.services.mpe_link import (\n"
              "            MpeLink,\n"
              "        )\n"
              "        from shared_engines.identity.con
