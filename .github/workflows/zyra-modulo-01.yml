name: zyra-q3-sbs-addonly

"on":
  workflow_dispatch:
  push:
    branches: ["**"]

permissions:
  contents: write

concurrency:
  group: zyra-sbs-add-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install toolchain
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install "cryptography>=42,<45" "pytest>=8,<9" "pytest-timeout>=2,<3"

      - name: Evidence (before)
        run: |
          set -euo pipefail
          wc -c apps/subastas/server.py apps/subastas/infrastructure/persistence/subastas_store.py

      - name: ADD CommerceStore (new file - original store untouched)
        run: |
          set -euo pipefail
          cat > apps/subastas/infrastructure/persistence/subastas_commerce.py <<'PYEOF'
          """SUBASTAS commerce layer - ADD-ONLY module."""
          from __future__ import annotations

          import threading
          import time
          import uuid

          PLATFORM_FEE_RATE = 0.05


          def _nid(prefix: str) -> str:
              return prefix + uuid.uuid4().hex[:10]


          class CommerceStore:
              """ADD-ONLY commerce layer over the existing
              SUBASTAS database."""

              def __init__(self, db, clock) -> None:
                  self._db = db
                  self._clock = clock
                  self._lock = threading.Lock()
                  self._ensure_schema()

              def _now(self) -> float:
                  try:
                      return float(self._clock.now())
                  except Exception:
                      return time.time()

              def _ensure_schema(self) -> None:
                  for ddl in (
                      "ALTER TABLE sbs_listings ADD COLUMN winner_account TEXT",
                      "ALTER TABLE sbs_listings ADD COLUMN final_price REAL",
                      "ALTER TABLE sbs_listings ADD COLUMN closed_at REAL",
                  ):
                      try:
                          self._db.execute(ddl)
                      except Exception:
                          pass
                  for ddl in (
                      "CREATE TABLE IF NOT EXISTS sbs_orders ("
                      " order_id TEXT PRIMARY KEY,"
                      " listing_id TEXT NOT NULL,"
                      " buyer_account TEXT NOT NULL,"
                      " buyer_zid TEXT,"
                      " seller_account TEXT NOT NULL,"
                      " seller_zid TEXT,"
                      " source TEXT NOT NULL,"
                      " subtotal REAL NOT NULL,"
                      " platform_fee REAL NOT NULL,"
                      " total REAL NOT NULL,"
                      " currency TEXT NOT NULL DEFAULT 'USD',"
                      " status TEXT NOT NULL,"
                      " created_at REAL NOT NULL,"
                      " updated_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_payments ("
                      " payment_id TEXT PRIMARY KEY,"
                      " order_id TEXT NOT NULL,"
                      " provider TEXT NOT NULL,"
                      " amount REAL NOT NULL,"
                      " currency TEXT NOT NULL DEFAULT 'USD',"
                      " status TEXT NOT NULL,"
                      " created_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_commissions ("
                      " commission_id TEXT PRIMARY KEY,"
                      " order_id TEXT NOT NULL,"
                      " rate REAL NOT NULL,"
                      " gross_amount REAL NOT NULL,"
                      " commission_amount REAL NOT NULL,"
                      " currency TEXT NOT NULL DEFAULT 'USD',"
                      " created_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_settlements ("
                      " settlement_id TEXT PRIMARY KEY,"
                      " order_id TEXT NOT NULL,"
                      " seller_account TEXT NOT NULL,"
                      " gross_amount REAL NOT NULL,"
                      " commission REAL NOT NULL,"
                      " net_amount REAL NOT NULL,"
                      " currency TEXT NOT NULL DEFAULT 'USD',"
                      " status TEXT NOT NULL,"
                      " created_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_shipments ("
                      " shipment_id TEXT PRIMARY KEY,"
                      " order_id TEXT NOT NULL,"
                      " carrier TEXT NOT NULL,"
                      " origin TEXT NOT NULL,"
                      " destination TEXT NOT NULL,"
                      " status TEXT NOT NULL,"
                      " created_at REAL NOT NULL,"
                      " updated_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_tracking_events ("
                      " event_id TEXT PRIMARY KEY,"
                      " shipment_id TEXT NOT NULL,"
                      " status TEXT NOT NULL,"
                      " location TEXT,"
                      " description TEXT,"
                      " occurred_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_reputation_events ("
                      " event_id TEXT PRIMARY KEY,"
                      " order_id TEXT,"
                      " subject_zid TEXT NOT NULL,"
                      " actor_zid TEXT NOT NULL,"
                      " kind TEXT NOT NULL,"
                      " evidence TEXT NOT NULL,"
                      " network_recorded INTEGER NOT NULL DEFAULT 0,"
                      " created_at REAL NOT NULL)",
                      "CREATE TABLE IF NOT EXISTS sbs_opportunities ("
                      " opportunity_id TEXT PRIMARY KEY,"
                      " title TEXT NOT NULL,"
                      " category TEXT,"
                      " purchase_price REAL NOT NULL,"
                      " shipping_cost REAL NOT NULL,"
                      " fees REAL NOT NULL,"
                      " estimated_sale_price REAL NOT NULL,"
                      " total_cost REAL NOT NULL,"
                      " expected_profit REAL NOT NULL,"
                      " margin_percent REAL NOT NULL,"
                      " demand_score REAL NOT NULL,"
                      " risk_score REAL NOT NULL,"
                      " opportunity_score REAL NOT NULL,"
                      " status TEXT NOT NULL,"
                      " created_at REAL NOT NULL)",
                  ):
                      self._db.execute(ddl)

              def _run(self, sql, params=()) -> None:
                  if not params:
                      self._db.execute(sql)
                      return
                  try:
                      self._db.execute(sql, params)
                      return
                  except TypeError:
                      self._db.execute(self._inline(sql, params))

              @staticmethod
              def _inline(sql, params) -> str:
                  parts = sql.split("?")
                  if len(parts) != len(params) + 1:
                      return sql
                  assembled = parts[0]
                  for index, value in enumerate(params):
                      assembled += CommerceStore._literal(value)
                      assembled += parts[index + 1]
                  return assembled

              @staticmethod
              def _literal(value) -> str:
                  if value is None:
                      return "NULL"
                  if isinstance(value, bool):
                      return "1" if value else "0"
                  if isinstance(value, (int, float)):
                      return repr(value)
                  text = str(value).replace("'", "''")
                  return "'" + text + "'"

              def _rows(self, sql) -> list:
                  for name in ("query", "fetchall", "fetch_all", "fetch", "select"):
                      fn = getattr(self._db, name, None)
                      if not callable(fn):
                          continue
                      try:
                          rows = fn(sql)
                          if rows is None:
                              continue
                          return list(rows)
                      except Exception:
                          continue
                  try:
                      cursor = self._db.execute(sql)
                  except Exception:
                      return []
                  if cursor is None:
                      return []
                  try:
                      return list(cursor.fetchall())
                  except Exception:
                      return []

              @staticmethod
              def _field(row, key, index):
                  if isinstance(row, dict):
                      return row.get(key)
                  try:
                      return row[index]
                  except Exception:
                      return None

              def persist_winner(self, *, listing_id: str) -> dict:
                  with self._lock:
                      rows = self._rows(
                          "SELECT bidder_account, amount FROM sbs_bids"
                          " WHERE listing_id = "
                          + self._literal(listing_id)
                          + " ORDER BY amount DESC LIMIT 1"
                      )
                      if not rows:
                          raise LookupError("no bids for: " + listing_id)
                      winner = str(self._field(rows[0], "w", 0))
                      price = float(self._field(rows[0], "p", 1))
                      now = self._now()
                      self._run(
                          "UPDATE sbs_listings SET"
                          " winner_account = "
                          + self._literal(winner)
                          + ", final_price = "
                          + self._literal(price)
                          + ", closed_at = "
                          + self._literal(now)
                          + " WHERE listing_id = "
                          + self._literal(listing_id)
                      )
                      return {
                          "listing_id": listing_id,
                          "winner_account": winner,
                          "final_price": price,
                      }

              def get_listing_full(self, listing_id: str):
                  rows = self._rows(
                      "SELECT listing_id, seller_account, seller_zid,"
                      " title, base_price, document_id, status,"
                      " winner_account, final_price, closed_at"
                      " FROM sbs_listings WHERE listing_id = "
                      + self._literal(listing_id)
                  )
                  if not rows:
                      return None
                  row = rows[0]
                  return {
                      "listing_id": self._field(row, "a", 0),
                      "seller_account": self._field(row, "b", 1),
                      "seller_zid": self._field(row, "c", 2),
                      "title": self._field(row, "d", 3),
                      "base_price": self._field(row, "e", 4),
                      "document_id": self._field(row, "f", 5),
                      "status": self._field(row, "g", 6),
                      "winner_account": self._field(row, "h", 7),
                      "final_price": self._field(row, "i", 8),
                      "closed_at": self._field(row, "j", 9),
                  }

              def account_zid(self, account_id: str):
                  rows = self._rows(
                      "SELECT zid FROM sbs_accounts WHERE account_id = "
                      + self._literal(account_id)
                  )
                  if not rows:
                      return None
                  zid = self._field(rows[0], "z", 0)
                  return str(zid) if zid is not None else None

              def create_order(
                  self, *, order_id, listing_id, buyer_account, source,
              ) -> dict:
                  with self._lock:
                      listing = self.get_listing_full(listing_id)
                      if listing is None:
                          raise LookupError("listing not found: " + listing_id)
                      if source == "auction":
                          if listing["status"] != "closed":
                              raise ValueError("auction not closed yet")
                          if listing["winner_account"] != buyer_account:
                              raise ValueError("buyer is not the auction winner")
                          subtotal = float(listing["final_price"] or 0)
                      else:
                          if listing["status"] != "open":
                              raise ValueError("listing is not open")
                          subtotal = float(listing["base_price"])
                      dup = self._rows(
                          "SELECT order_id FROM sbs_orders WHERE listing_id = "
                          + self._literal(listing_id)
                      )
                      if dup:
                          raise ValueError("order already exists for this listing")
                      fee = round(subtotal * PLATFORM_FEE_RATE, 2)
                      now = self._now()
                      self._run(
                          "INSERT INTO sbs_orders ("
                          " order_id, listing_id, buyer_account, buyer_zid,"
                          " seller_account, seller_zid, source, subtotal,"
                          " platform_fee, total, currency, status,"
                          " created_at, updated_at"
                          ") VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?,"
                          " 'USD', 'created', ?, ?)",
                          (
                              order_id,
                              listing_id,
                              buyer_account,
                              self.account_zid(buyer_account),
                              listing["seller_account"],
                              listing["seller_zid"],
                              source,
                              subtotal,
                              fee,
                              subtotal,
                              now,
                              now,
                          ),
                      )
                      return self.get_order(order_id)

              def get_order(self, order_id: str) -> dict:
                  rows = self._rows(
                      "SELECT order_id, listing_id, buyer_account,"
                      " buyer_zid, seller_account, seller_zid, source,"
                      " subtotal, platform_fee, total, currency, status,"
                      " created_at, updated_at FROM sbs_orders"
                      " WHERE order_id = "
                      + self._literal(order_id)
                  )
                  if not rows:
                      raise LookupError("order not found: " + order_id)
                  row = rows[0]
                  order = {
                      "order_id": self._field(row, "a", 0),
                      "listing_id": self._field(row, "b", 1),
                      "buyer_account": self._field(row, "c", 2),
                      "buyer_zid": self._field(row, "d", 3),
                      "seller_account": self._field(row, "e", 4),
                      "seller_zid": self._field(row, "f", 5),
                      "source": self._field(row, "g", 6),
                      "subtotal": self._field(row, "h", 7),
                      "platform_fee": self._field(row, "i", 8),
                      "total": self._field(row, "j", 9),
                      "currency": self._field(row, "k", 10),
                      "status": self._field(row, "l", 11),
                      "payments": [],
                      "commission": None,
                      "settlement": None,
                      "shipment": None,
                  }
                  pays = self._rows(
                      "SELECT payment_id, provider, amount, status"
                      " FROM sbs_payments WHERE order_id = "
                      + self._literal(order_id)
                  )
                  for p in pays:
                      order["payments"].append(
                          {
                              "payment_id": self._field(p, "a", 0),
                              "provider": self._field(p, "b", 1),
                              "amount": self._field(p, "c", 2),
                              "status": self._field(p, "d", 3),
                          }
                      )
                  com = self._rows(
                      "SELECT commission_id, rate, commission_amount"
                      " FROM sbs_commissions WHERE order_id = "
                      + self._literal(order_id)
                  )
                  if com:
                      order["commission"] = {
                          "commission_id": self._field(com[0], "a", 0),
                          "rate": self._field(com[0], "b", 1),
                          "commission_amount": self._field(com[0], "c", 2),
                      }
                  stl = self._rows(
                      "SELECT settlement_id, gross_amount, commission,"
                      " net_amount, status FROM sbs_settlements"
                      " WHERE order_id = "
                      + self._literal(order_id)
                  )
                  if stl:
                      order["settlement"] = {
                          "settlement_id": self._field(stl[0], "a", 0),
                          "gross_amount": self._field(stl[0], "b", 1),
                          "commission": self._field(stl[0], "c", 2),
                          "net_amount": self._field(stl[0], "d", 3),
                          "status": self._field(stl[0], "e", 4),
                      }
                  shp = self._rows(
                      "SELECT shipment_id, carrier, origin, destination,"
                      " status FROM sbs_shipments WHERE order_id = "
                      + self._literal(order_id)
                  )
                  if shp:
                      order["shipment"] = {
                          "shipment_id": self._field(shp[0], "a", 0),
                          "carrier": self._field(shp[0], "b", 1),
                          "origin": self._field(shp[0], "c", 2),
                          "destination": self._field(shp[0], "d", 3),
                          "status": self._field(shp[0], "e", 4),
                      }
                  return order

              def pay_order(self, *, order_id, provider) -> dict:
                  with self._lock:
                      order = self.get_order(order_id)
                      if order["status"] != "created":
                          raise ValueError("order is not payable")
                      now = self._now()
                      self._run(
                          "INSERT INTO sbs_payments ("
                          " payment_id, order_id, provider, amount,"
                          " currency, status, created_at"
                          ") VALUES (?, ?, ?, ?, 'USD', 'captured', ?)",
                          (_nid("PAY-"), order_id, provider, order["total"], now),
                      )
                      self._run(
                          "INSERT INTO sbs_commissions ("
                          " commission_id, order_id, rate, gross_amount,"
                          " commission_amount, currency, created_at"
                          ") VALUES (?, ?, ?, ?, ?, 'USD', ?)",
                          (
                              _nid("COM-"),
                              order_id,
                              PLATFORM_FEE_RATE,
                              order["total"],
                              order["platform_fee"],
                              now,
                          ),
                      )
                      self._run(
                          "UPDATE sbs_orders SET status = 'paid', updated_at = "
                          + self._literal(now)
                          + " WHERE order_id = "
                          + self._literal(order_id)
                      )
                      return self.get_order(order_id)

              def settle_order(self, *, order_id) -> dict:
                  with self._lock:
                      order = self.get_order(order_id)
                      if order["status"] != "paid":
                          raise ValueError("order is not settleable")
                      net = round(order["total"] - order["platform_fee"], 2)
                      self._run(
                          "INSERT INTO sbs_settlements ("
                          " settlement_id, order_id, seller_account,"
                          " gross_amount, commission, net_amount,"
                          " currency, status, created_at"
                          ") VALUES (?, ?, ?, ?, ?, ?, 'USD', 'settled', ?)",
                          (
                              _nid("SET-"),
                              order_id,
                              order["seller_account"],
                              order["total"],
                              order["platform_fee"],
                              net,
                              self._now(),
                          ),
                      )
                      self._run(
                          "UPDATE sbs_orders SET status = 'settled',"
                          " updated_at = "
                          + self._literal(self._now())
                          + " WHERE order_id = "
                          + self._literal(order_id)
                      )
                      return self.get_order(order_id)

              def create_shipment(
                  self, *, shipment_id, order_id, carrier, origin, destination,
              ) -> dict:
                  with self._lock:
                      order = self.get_order(order_id)
                      if order["status"] != "settled":
                          raise ValueError("order is not shippable")
                      now = self._now()
                      self._run(
                          "INSERT INTO sbs_shipments ("
                          " shipment_id, order_id, carrier, origin,"
                          " destination, status, created_at, updated_at"
                          ") VALUES (?, ?, ?, ?, ?, 'created', ?, ?)",
                          (shipment_id, order_id, carrier, origin, destination, now, now),
                      )
                      self._run(
                          "UPDATE sbs_orders SET status = 'shipped', updated_at = "
                          + self._literal(now)
                          + " WHERE order_id = "
                          + self._literal(order_id)
                      )
                      return self.get_shipment(shipment_id)

              def get_shipment(self, shipment_id: str) -> dict:
                  rows = self._rows(
                      "SELECT shipment_id, order_id, carrier, origin,"
                      " destination, status FROM sbs_shipments"
                      " WHERE shipment_id = "
                      + self._literal(shipment_id)
                  )
                  if not rows:
                      raise LookupError("shipment not found: " + shipment_id)
                  row = rows[0]
                  events = self._rows(
                      "SELECT event_id, status, location, description"
                      " FROM sbs_tracking_events WHERE shipment_id = "
                      + self._literal(shipment_id)
                      + " ORDER BY occurred_at"
                  )
                  tracking = []
                  for e in events:
                      tracking.append(
                          {
                              "event_id": self._field(e, "a", 0),
                              "status": self._field(e, "b", 1),
                              "location": self._field(e, "c", 2),
                              "description": self._field(e, "d", 3),
                          }
                      )
                  return {
                      "shipment_id": self._field(row, "a", 0),
                      "order_id": self._field(row, "b", 1),
                      "carrier": self._field(row, "c", 2),
                      "origin": self._field(row, "d", 3),
                      "destination": self._field(row, "e", 4),
                      "status": self._field(row, "f", 5),
                      "tracking": tracking,
                  }

              def add_tracking(
                  self, *, shipment_id, status, location, description,
              ) -> dict:
                  with self._lock:
                      current = self.get_shipment(shipment_id)
                      if current["status"] == "delivered":
                          raise ValueError("shipment already delivered")
                      self._run(
                          "INSERT INTO sbs_tracking_events ("
                          " event_id, shipment_id, status, location,"
                          " description, occurred_at"
                          ") VALUES (?, ?, ?, ?, ?, ?)",
                          (_nid("TRK-"), shipment_id, status, location, description, self._now()),
                      )
                      self._run(
                          "UPDATE sbs_shipments SET status = "
                          + self._literal(status)
                          + ", updated_at = "
                          + self._literal(self._now())
                          + " WHERE shipment_id = "
                          + self._literal(shipment_id)
                      )
                      return self.get_shipment(shipment_id)

              def confirm_delivery(self, *, shipment_id) -> dict:
                  with self._lock:
                      current = self.get_shipment(shipment_id)
                      if current["status"] == "delivered":
                          raise ValueError("shipment already delivered")
                      now = self._now()
                      self._run(
                          "INSERT INTO sbs_tracking_events ("
                          " event_id, shipment_id, status, location,"
                          " description, occurred_at"
                          ") VALUES (?, ?, 'delivered', NULL,"
                          " 'entregado al comprador', ?)",
                          (_nid("TRK-"), shipment_id, now),
                      )
                      self._run(
                          "UPDATE sbs_shipments SET status = 'delivered',"
                          " updated_at = "
                          + self._literal(now)
                          + " WHERE shipment_id = "
                          + self._literal(shipment_id)
                      )
                      self._run(
                          "UPDATE sbs_orders SET status = 'delivered',"
                          " updated_at = "
                          + self._literal(now)
                          + " WHERE order_id = "
                          + self._literal(current["order_id"])
                      )
                      return self.get_shipment(shipment_id)

              def record_rep_event(
                  self, *, order_id, subject_zid, actor_zid, kind, evidence,
                  network_recorded,
              ) -> dict:
                  self._run(
                      "INSERT INTO sbs_reputation_events ("
                      " event_id, order_id, subject_zid, actor_zid,"
                      " kind, evidence, network_recorded, created_at"
                      ") VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
                      (
                          _nid("REP-"),
                          order_id,
                          subject_zid,
                          actor_zid,
                          kind,
                          evidence,
                          1 if network_recorded else 0,
                          self._now(),
                      ),
                  )
                  return {"recorded": True}

              def create_opportunity(
                  self, *, opportunity_id, title, category, purchase_price,
                  shipping_cost, fees, estimated_sale_price, demand_score,
                  risk_score,
              ) -> dict:
                  total_cost = round(purchase_price + shipping_cost + fees, 2)
                  if total_cost <= 0:
                      raise ValueError("total cost must be positive")
                  expected_profit = round(estimated_sale_price - total_cost, 2)
                  margin_percent = round(expected_profit / total_cost * 100, 2)
                  score = round(
                      margin_percent * 0.5
                      + demand_score * 30
                      + (1 - risk_score) * 20,
                      2,
                  )
                  self._run(
                      "INSERT INTO sbs_opportunities ("
                      " opportunity_id, title, category, purchase_price,"
                      " shipping_cost, fees, estimated_sale_price,"
                      " total_cost, expected_profit, margin_percent,"
                      " demand_score, risk_score, opportunity_score,"
                      " status, created_at"
                      ") VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'open', ?)",
                      (
                          opportunity_id,
                          title,
                          category,
                          purchase_price,
                          shipping_cost,
                          fees,
                          estimated_sale_price,
                          total_cost,
                          expected_profit,
                          margin_percent,
                          demand_score,
                          risk_score,
                          score,
                          self._now(),
                      ),
                  )
                  return {
                      "opportunity_id": opportunity_id,
                      "title": title,
                      "total_cost": total_cost,
                      "expected_profit": expected_profit,
                      "margin_percent": margin_percent,
                      "opportunity_score": score,
                  }

              def list_opportunities(self) -> list:
                  rows = self._rows(
                      "SELECT opportunity_id, title, category,"
                      " purchase_price, estimated_sale_price, total_cost,"
                      " expected_profit, margin_percent, opportunity_score,"
                      " status FROM sbs_opportunities ORDER BY created_at"
                  )
                  result = []
                  for row in rows:
                      result.append(
                          {
                              "opportunity_id": self._field(row, "a", 0),
                              "title": self._field(row, "b", 1),
                              "category": self._field(row, "c", 2),
                              "purchase_price": self._field(row, "d", 3),
                              "estimated_sale_price": self._field(row, "e", 4),
                              "total_cost": self._field(row, "f", 5),
                              "expected_profit": self._field(row, "g", 6),
                              "margin_percent": self._field(row, "h", 7),
                              "opportunity_score": self._field(row, "i", 8),
                              "status": self._field(row, "j", 9),
                          }
                      )
                  return result

              def summary(self) -> dict:
                  orders = self._rows("SELECT COUNT(*) FROM sbs_orders")
                  delivered = self._rows(
                      "SELECT COUNT(*) FROM sbs_orders WHERE status = 'delivered'"
                  )
                  opps = self._rows("SELECT COUNT(*) FROM sbs_opportunities")
                  return {
                      "orders_total": int(self._field(orders[0], "c", 0) or 0) if orders else 0,
                      "orders_delivered": int(self._field(delivered[0], "c", 0) or 0) if delivered else 0,
                      "opportunities": int(self._field(opps[0], "c", 0) or 0) if opps else 0,
                  }
          PYEOF
          python -m py_compile apps/subastas/infrastructure/persistence/subastas_commerce.py
          echo "commerce layer added (original store untouched)"

      - name: ADD server patches (multi-line anchors matching real file)
        run: |
          set -euo pipefail
          PYTHONPATH="$PWD" python - <<'PYEOF'
          from pathlib import Path

          path = Path("apps/subastas/server.py")
          source = path.read_text(encoding="utf-8")

          def apply(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(
                      "PATCH FAIL " + label + " count=" + str(count)
                  )
              return text.replace(old, new, 1)

          IMPORT_ANCHOR = "\n".join([
              "from apps.subastas.infrastructure.persistence.subastas_store import (",
              "    SubastasStore,",
              "    new_bid_id,",
              "    new_listing_id,",
              ")",
          ])
          IMPORT_NEW = "\n".join([
              IMPORT_ANCHOR,
              "from apps.subastas.infrastructure.persistence.subastas_commerce import (",
              "    CommerceStore,",
              "    _nid,",
              ")",
              "from apps.subastas.services.subastas_link import SubastasLink",
          ])

          GET_ANCHOR = "\n".join([
              '            if path == (',
              '                "/subastas/api/summary"',
              '            ):',
          ])
          GET_ROUTES = "\n".join([
              '            if path.startswith("/subastas/api/listings/"):',
              '                listing = self.commerce.get_listing_full(',
              '                    path[len("/subastas/api/listings/"):]',
              '                )',
              '                if listing is None:',
              '                    self._send_json(',
              '                        200,',
              '                        {"ok": False, "error": "unknown listing"},',
              '                    )',
              '                    return',
              '                self._send_json(200, {"ok": True, "data": listing})',
              '                return',
              '            if path == "/subastas/api/opportunities":',
              '                self._send_json(',
              '                    200,',
              '                    {',
              '                        "ok": True,',
              '                        "data": {',
              '                            "opportunities": self.commerce.list_opportunities()',
              '                        },',
              '                    },',
              '                )',
              '                return',
              '            if path == "/subastas/revision":',
              '                self._send_html(200, self._revision_html())',
              '                return',
              '            if path.startswith("/subastas/api/orders/"):',
              '                self._send_json(',
              '                    200,',
              '                    {"ok": True, "data": self.commerce.get_order(',
              '                        path[len("/subastas/api/orders/"):]',
              '                    )},',
              '                )',
              '                return',
              '            if path.startswith("/subastas/api/shipments/"):',
              '                self._send_json(',
              '                    200,',
              '                    {"ok": True, "data": self.commerce.get_shipment(',
              '                        path[len("/subastas/api/shipments/"):]',
              '                    )},',
              '                )',
              '                return',
              GET_ANCHOR,
          ])

          POST_ANCHOR = "\n".join([
              '            if path == (',
              '                "/subastas/close"',
              '            ):',
          ])
          POST_ROUTES = "\n".join([
              '            if path == "/subastas/api/orders":',
              '                self._create_order_json()',
              '                return',
              '            if path == "/subastas/api/reputation/mutual":',
              '                self._mutual_reputation_json()',
              '                return',
              '            if path == "/subastas/api/radar/scan":',
              '                self._radar_scan_json()',
              '                return',
              '            if path.startswith("/subastas/api/orders/"):',
              '                parts = path[len("/subastas/api/orders/"):].split("/")',
              '                self._order_action(',
              '                    parts[0],',
              '                    parts[1] if len(parts) > 1 else "",',
              '                )',
              '                return',
              '            if path.startswith("/subastas/api/shipments/"):',
              '                parts = path[len("/subastas/api/shipments/"):].split("/")',
              '                self._shipment_action(',
              '                    parts[0],',
              '                    parts[1] if len(parts) > 1 else "",',
              '                )',
              '                return',
              POST_ANCHOR,
          ])

          METHODS = "\n".join([
              '    def _create_order_json(self) -> None:',
              '        doc = self._read_json()',
              '        if doc is None:',
              '            self._send_json(400, {"ok": False, "error": "invalid JSON"})',
              '            return',
              '        source = str(doc.get("source", "direct"))',
              '        if source == "auction":',
              '            self.commerce.persist_winner(',
              '                listing_id=str(doc.get("listing_id", ""))',
              '            )',
              '        order = self.commerce.create_order(',
              '            order_id=_nid("ORD-"),',
              '            listing_id=str(doc.get("listing_id", "")),',
              '            buyer_account=str(doc.get("buyer_account", "")),',
              '            source=source,',
              '        )',
              '        self._send_json(201, {"ok": True, "data": order})',
              '',
              '    def _order_action(self, order_id: str, action: str) -> None:',
              '        doc = self._read_json() or {}',
              '        if action == "pay":',
              '            order = self.commerce.pay_order(',
              '                order_id=order_id,',
              '                provider=str(doc.get("provider", "wallet")),',
              '            )',
              '            self._send_json(200, {"ok": True, "data": order})',
              '            return',
              '        if action == "settle":',
              '            order = self.commerce.settle_order(order_id=order_id)',
              '            self._send_json(200, {"ok": True, "data": order})',
              '            return',
              '        if action == "ship":',
              '            self.commerce.create_shipment(',
              '                shipment_id=_nid("SHP-"),',
              '                order_id=order_id,',
              '                carrier=str(doc.get("carrier", "zyra-express")),',
              '                origin=str(doc.get("origin", "SV")),',
              '                destination=str(doc.get("destination", "SV")),',
              '            )',
              '            self._send_json(',
              '                200,',
              '                {"ok": True, "data": self.commerce.get_order(order_id)},',
              '            )',
              '            return',
              '        self._send_json(',
              '            400,',
              '            {"ok": False, "error": "unknown order action: " + action},',
              '        )',
              '',
              '    def _shipment_action(self, shipment_id: str, action: str) -> None:',
              '        doc = self._read_json() or {}',
              '        if action == "track":',
              '            shipment = self.commerce.add_tracking(',
              '                shipment_id=shipment_id,',
              '                status=str(doc.get("status", "in_transit")),',
              '                location=str(doc.get("location", "")),',
              '                description=str(doc.get("description", "")),',
              '            )',
              '            self._send_json(200, {"ok": True, "data": shipment})',
              '            return',
              '        if action == "deliver":',
              '            shipment = self.commerce.confirm_delivery(',
              '                shipment_id=shipment_id,',
              '            )',
              '            self._send_json(200, {"ok": True, "data": shipment})',
              '            return',
              '        self._send_json(',
              '            400,',
              '            {"ok": False, "error": "unknown shipment action: " + action},',
              '        )',
              '',
              '    def _mutual_reputation_json(self) -> None:',
              '        doc = self._read_json()',
              '        if doc is None:',
              '            self._send_json(400, {"ok": False, "error": "invalid JSON"})',
              '            return',
              '        order_id = str(doc.get("order_id", ""))',
              '        buyer_account = str(doc.get("buyer_account", ""))',
              '        seller_account = str(doc.get("seller_account", ""))',
              '        evidence = str(',
              '            doc.get("evidence", "")',
              '            or "transaccion completada en SUBASTAS"',
              '        )',
              '        order = self.commerce.get_order(order_id)',
              '        buyer_zid = self.commerce.account_zid(buyer_account)',
              '        seller_zid = self.commerce.account_zid(seller_account)',
              '        if not buyer_zid or not seller_zid:',
              '            raise ValueError("accounts must have network ZIDs")',
              '',
              '        def _network(rep) -> bool:',
              '            try:',
              '                if not rep[0]:',
              '                    return False',
              '                data = rep[1]',
              '                if isinstance(data, dict) and data.get("recorded") == "local":',
              '                    return False',
              '                return True',
              '            except Exception:',
              '                return False',
              '',
              '        rep_b2s = self.link.give_reputation(',
              '            subject_zid=seller_zid,',
              '            actor_zid=buyer_zid,',
              '            kind="positive",',
              '            evidence=evidence,',
              '        )',
              '        rep_s2b = self.link.give_reputation(',
              '            subject_zid=buyer_zid,',
              '            actor_zid=seller_zid,',
              '            kind="positive",',
              '            evidence=evidence,',
              '        )',
              '        self.commerce.record_rep_event(',
              '            order_id=order_id,',
              '            subject_zid=seller_zid,',
              '            actor_zid=buyer_zid,',
              '            kind="positive",',
              '            evidence=evidence,',
              '            network_recorded=_network(rep_b2s),',
              '        )',
              '        self.commerce.record_rep_event(',
              '            order_id=order_id,',
              '            subject_zid=buyer_zid,',
              '            actor_zid=seller_zid,',
              '            kind="positive",',
              '            evidence=evidence,',
              '            network_recorded=_network(rep_s2b),',
              '        )',
              '        hist_s = self.link.record_history(',
              '            seller_zid, "venta completada", "orden " + order_id',
              '        )',
              '        hist_b = self.link.record_history(',
              '            buyer_zid, "compra completada", "orden " + order_id',
              '        )',
              '        self._send_json(',
              '            200,',
              '            {',
              '                "ok": True,',
              '                "data": {',
              '                    "order_id": order_id,',
              '                    "buyer_to_seller_network": _network(rep_b2s),',
              '                    "seller_to_buyer_network": _network(rep_s2b),',
              '                    "history_seller_recorded": bool(hist_s[0]),',
              '                    "history_buyer_recorded": bool(hist_b[0]),',
              '                },',
              '            },',
              '        )',
              '',
              '    def _radar_scan_json(self) -> None:',
              '        doc = self._read_json()',
              '        if doc is None:',
              '            self._send_json(400, {"ok": False, "error": "invalid JSON"})',
              '            return',
              '        purchase = float(doc.get("purchase_price", 0))',
              '        sale = float(doc.get("estimated_sale_price", 0))',
              '        if purchase <= 0 or sale <= 0:',
              '            raise ValueError(',
              '                "purchase_price and estimated_sale_price must be positive"',
              '            )',
              '        opportunity = self.commerce.create_opportunity(',
              '            opportunity_id=_nid("OPP-"),',
              '            title=str(doc.get("title", "oportunidad")),',
              '            category=str(doc.get("category", "general")),',
              '            purchase_price=purchase,',
              '            shipping_cost=float(doc.get("shipping_cost", 0)),',
              '            fees=float(doc.get("fees", 0)),',
              '            estimated_sale_price=sale,',
              '            demand_score=float(doc.get("demand_score", 0.5)),',
              '            risk_score=float(doc.get("risk_score", 0.5)),',
              '        )',
              '        self._send_json(201, {"ok": True, "data": opportunity})',
              '',
              '    def _revision_html(self) -> str:',
              '        summary = self.commerce.summary()',
              '        return (',
              '            "<html><body><h1>Revision SUBASTAS</h1>"',
              '            "<p>ordenes: " + str(summary["orders_total"]) + "</p>"',
              '            "<p>entregadas: " + str(summary["orders_delivered"]) + "</p>"',
              '            "<p>oportunidades radar: " + str(summary["opportunities"]) + "</p>"',
              '            "<a href=\'/subastas\'>Inicio</a>"',
              '            "</body></html>"',
              '        )',
              '',
          ])

          source = apply(source, IMPORT_ANCHOR, IMPORT_NEW, "import")
          source = apply(
              source,
              GET_ANCHOR,
              GET_ROUTES,
              "get-routes",
          )
          source = apply(
              source,
              POST_ANCHOR,
              POST_ROUTES,
              "post-routes",
          )
          source = apply(
              source,
              '    def _home(self) -> str:',
              METHODS + "\n" + '    def _home(self) -> str:',
              "methods",
          )

          path.write_text(source, encoding="utf-8")
          print("ALL 4 ADD-PATCHES APPLIED")
          PYEOF
          python -m py_compile apps/subastas/server.py
          echo "server patched and compiles"

      - name: Bind commerce into serve_subastas
        run: |
          set -euo pipefail
          PYTHONPATH="$PWD" python - <<'PYEOF'
          from pathlib import Path

          path = Path("apps/subastas/server.py")
          source = path.read_text(encoding="utf-8")

          def apply(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit("PATCH FAIL " + label + " count=" + str(count))
              return text.replace(old, new, 1)

          OLD = "\n".join([
              'def serve_subastas(',
              '    store: SubastasStore,',
              '    client: object,',
              '    *,',
              '    host: str = "127.0.0.1",',
              '    port: int = 0,',
              ') -> ThreadingHTTPServer:',
              '    handler = type(',
              '        "BoundSubastasHandler",',
              '        (SubastasApiHandler,),',
              '        {',
              '            "store": store,',
              '            "net_client": client,',
              '        },',
              '    )',
          ])

          NEW = "\n".join([
              'def serve_subastas(',
              '    store: SubastasStore,',
              '    client: object,',
              '    *,',
              '    host: str = "127.0.0.1",',
              '    port: int = 0,',
              ') -> ThreadingHTTPServer:',
              '    handler = type(',
              '        "BoundSubastasHandler",',
              '        (SubastasApiHandler,),',
              '        {',
              '            "store": store,',
              '            "net_client": client,',
              '            "commerce": CommerceStore(',
              '                store._db, store._clock',
              '            ),',
              '            "link": SubastasLink(client),',
              '        },',
              '    )',
          ])

          source = apply(source, OLD, NEW, "serve-bind")
          path.write_text(source, encoding="utf-8")
          print("SERVE BIND APPLIED")
          PYEOF
          python -m py_compile apps/subastas/server.py
          echo "bind applied and compiles"

      - name: Internal probe (commerce ADD-ONLY layer)
        run: |
          set -euo pipefail
          PYTHONPATH="$PWD" python - <<'PYEOF'
          import sqlite3
          import tempfile
          from pathlib import Path

          from apps.subastas.infrastructure.persistence.subastas_store import SubastasStore
          from apps.subastas.infrastructure.persistence.subastas_commerce import CommerceStore

          class Clock:
              def now(self):
                  return 1000000.0

          tmp = Path(tempfile.mkdtemp())
          db = sqlite3.connect(tmp / "probe.db")
          db.row_factory = sqlite3.Row
          store = SubastasStore(db, Clock())
          commerce = CommerceStore(db, Clock())
          store.add_account(account_id="SBS-a", zid="ZID-a", name="A")
          store.add_account(account_id="SBS-b", zid="ZID-b", name="B")
          store.add_listing(
              listing_id="LST-1",
              seller_account="SBS-a",
              seller_zid="ZID-a",
              title="t",
              description="d",
              base_price=100.0,
              document_id="DOC-x",
          )
          store.place_bid(
              bid_id="BID-1",
              listing_id="LST-1",
              bidder_account="SBS-b",
              amount=650.0,
          )
          closed = store.close_listing(listing_id="LST-1", seller_account="SBS-a")
          assert closed["winner_account"] == "SBS-b"
          persisted = commerce.persist_winner(listing_id="LST-1")
          assert persisted["winner_account"] == "SBS-b"
          full = commerce.get_listing_full("LST-1")
          assert full["winner_account"] == "SBS-b"
          assert full["final_price"] == 650.0
          assert full["closed_at"] is not None
          order = commerce.create_order(
              order_id="ORD-1",
              listing_id="LST-1",
              buyer_account="SBS-b",
              source="auction",
          )
          assert order["subtotal"] == 650.0
          assert order["platform_fee"] == 32.5
          paid = commerce.pay_order(order_id="ORD-1", provider="wallet")
          assert paid["status"] == "paid"
          try:
              commerce.pay_order(order_id="ORD-1", provider="wallet")
              raise AssertionError("double pay must fail")
          except ValueError:
              pass
          settled = commerce.settle_order(order_id="ORD-1")
          assert settled["settlement"]["net_amount"] == 617.5
          shipment = commerce.create_shipment(
              shipment_id="SHP-1",
              order_id="ORD-1",
              carrier="zyra-express",
              origin="San Salvador",
              destination="Santa Ana",
          )
          assert shipment["status"] == "created"
          tracked = commerce.add_tracking(
              shipment_id="SHP-1",
              status="in_transit",
              location="Ruta 1",
              description="en camino",
          )
          assert tracked["status"] == "in_transit"
          delivered = commerce.confirm_delivery(shipment_id="SHP-1")
          assert delivered["status"] == "delivered"
          assert commerce.get_order("ORD-1")["status"] == "delivered"
          opp = commerce.create_opportunity(
              opportunity_id="OPP-1",
              title="Moto",
              category="vehiculos",
              purchase_price=100.0,
              shipping_cost=10.0,
              fees=5.0,
              estimated_sale_price=200.0,
              demand_score=0.8,
              risk_score=0.2,
          )
          assert opp["margin_percent"] == 73.91
          assert opp["opportunity_score"] == 76.95
          summary = commerce.summary()
          assert summary["orders_total"] == 1
          assert summary["orders_delivered"] == 1
          assert summary["opportunities"] == 1
          print("PROBE OK: ADD-ONLY commerce layer")
          db.close()
          PYEOF

      - name: Write commerce e2e tests (new file - old test untouched)
        run: |
          set -euo pipefail
          cat > apps/subastas/tests/test_subastas_commerce_e2e.py <<'PYEOF'
          """SUBASTAS commerce e2e ADD-ONLY - winner persisted,
          order, payment, commission, settlement, shipment,
          tracking, delivery, mutual reputation, radar VIP,
          offline resilience. Original routes/tests untouched."""
          from __future__ import annotations

          import json
          import socket
          import threading
          from pathlib import Path
          from urllib.request import Request, urlopen

          socket.setdefaulttimeout(20)

          from shared_engines.common.clocks import FrozenClock
          from shared_engines.runtime.capabilities import ZyraCapabilities
          from shared_engines.runtime.combined_api import serve_combined
          from shared_engines.runtime.config import RuntimeConfig
          from shared_engines.runtime.kernel import ZyraKernel
          from shared_engines.storage.database import SQLiteAdapter
          from shared_engines.verification.signatures import Ed25519Signer

          from apps.subastas.infrastructure.network.network_client import NetworkClient
          from apps.subastas.infrastructure.persistence.subastas_store import SubastasStore
          from apps.subastas.server import serve_subastas
          from apps.subastas.services.subastas_link import SubastasLink


          class _Eco:
              def __init__(self, tmp_path: Path) -> None:
                  self.net_db = SQLiteAdapter(tmp_path / "network.db")
                  signer, _ = Ed25519Signer.generate()
                  self.kernel = ZyraKernel(
                      db=self.net_db,
                      clock=FrozenClock(),
                      signer=signer,
                      config=RuntimeConfig(host="127.0.0.1", port=0, api_token=None),
                  )
                  self.kernel.bootstrap_root()
                  self.caps = ZyraCapabilities(
                      self.net_db,
                      FrozenClock(),
                      identity=self.kernel.identity,
                      signer=signer,
                  )
                  self.net_server = serve_combined(
                      self.kernel, self.caps, host="127.0.0.1", port=0
                  )
                  threading.Thread(
                      target=self.net_server.serve_forever, daemon=True
                  ).start()
                  self.net_base = (
                      "http://127.0.0.1:"
                      f"{self.net_server.server_address[1]}"
                  )
                  self.client = NetworkClient(self.net_base, max_retries=1)
                  self.link = SubastasLink(self.client)
                  self.sbs_db = SQLiteAdapter(tmp_path / "sbs.db")
                  self.store = SubastasStore(self.sbs_db, FrozenClock())
                  self.sbs_server = serve_subastas(self.store, self.client)
                  threading.Thread(
                      target=self.sbs_server.serve_forever, daemon=True
                  ).start()
                  self.sbs_base = (
                      "http://127.0.0.1:"
                      f"{self.sbs_server.bound_port}"
                  )

              def close(self) -> None:
                  self.sbs_server.shutdown()
                  self.sbs_server.server_close()
                  self.net_server.shutdown()
                  self.net_server.server_close()
                  self.sbs_db.close()
                  self.net_db.close()


          def _pf(url: str, doc: dict) -> str:
              request = Request(
                  url,
                  data=(
                      "&".join(str(k) + "=" + str(v) for k, v in doc.items())
                  ).encode("utf-8"),
                  method="POST",
              )
              with urlopen(request, timeout=15) as r:
                  return r.read().decode("utf-8")


          def _pj(url: str, doc: dict) -> dict:
              request = Request(
                  url,
                  data=json.dumps(doc).encode("utf-8"),
                  headers={"Content-Type": "application/json"},
                  method="POST",
              )
              with urlopen(request, timeout=15) as r:
                  body = json.loads(r.read().decode("utf-8"))
              assert isinstance(body, dict)
              return body


          def _gj(url: str) -> dict:
              with urlopen(url, timeout=15) as r:
                  body = json.loads(r.read().decode("utf-8"))
              assert isinstance(body, dict)
              return body


          def _h(url: str) -> str:
              with urlopen(url, timeout=15) as r:
                  return r.read().decode("utf-8")


          def _accounts(eco) -> None:
              reg_s = eco.link.register_account("Vendedor Uno")
              seller_zid = str(reg_s[1].get("zid"))
              eco.store.add_account(
                  account_id="SBS-seller01", zid=seller_zid, name="Vendedor Uno"
              )
              reg_b = eco.link.register_account("Comprador Uno")
              buyer_zid = str(reg_b[1].get("zid"))
              eco.store.add_account(
                  account_id="SBS-buyer001", zid=buyer_zid, name="Comprador Uno"
              )


          def test_1_auction_full_commerce(tmp_path: Path) -> None:
              eco = _Eco(tmp_path)
              try:
                  assert eco.link.register_app()[0] is True
                  _accounts(eco)
                  listing_html = _pf(
                      eco.sbs_base + "/subastas/listing",
                      {
                          "seller_account": "SBS-seller01",
                          "title": "Moto usada 2019",
                          "description": "buena",
                          "base_price": 500,
                      },
                  )
                  assert "SELLADO" in listing_html
                  listing_id = eco.store.list_open()[0]["listing_id"]
                  body = _pj(
                      eco.sbs_base + "/subastas/api/bids",
                      {
                          "listing_id": listing_id,
                          "bidder_account": "SBS-buyer001",
                          "amount": 650,
                      },
                  )
                  assert body["ok"] is True
                  eco.store.close_listing(
                      listing_id=listing_id, seller_account="SBS-seller01"
                  )
                  order = _pj(
                      eco.sbs_base + "/subastas/api/orders",
                      {
                          "listing_id": listing_id,
                          "buyer_account": "SBS-buyer001",
                          "source": "auction",
                      },
                  )["data"]
                  order_id = order["order_id"]
                  assert order["subtotal"] == 650.0
                  assert order["platform_fee"] == 32.5
                  assert order["status"] == "created"
                  listing_after = _gj(
                      eco.sbs_base
                      + "/subastas/api/listings/"
                      + listing_id
                  )["data"]
                  assert listing_after["winner_account"] == "SBS-buyer001"
                  assert listing_after["final_price"] == 650.0
                  assert listing_after["closed_at"] is not None
                  paid = _pj(
                      eco.sbs_base + "/subastas/api/orders/" + order_id + "/pay",
                      {"provider": "wallet"},
                  )["data"]
                  assert paid["status"] == "paid"
                  assert paid["commission"]["commission_amount"] == 32.5
                  settled = _pj(
                      eco.sbs_base
                      + "/subastas/api/orders/"
                      + order_id
                      + "/settle",
                      {},
                  )["data"]
                  assert settled["settlement"]["net_amount"] == 617.5
                  shipped = _pj(
                      eco.sbs_base
                      + "/subastas/api/orders/"
                      + order_id
                      + "/ship",
                      {
                          "carrier": "zyra-express",
                          "origin": "San Salvador",
                          "destination": "Santa Ana",
                      },
                  )["data"]
                  assert shipped["status"] == "shipped"
                  assert shipped["shipment"]["status"] == "created"
                  shipment_id = shipped["shipment"]["shipment_id"]
                  tracked = _pj(
                      eco.sbs_base
                      + "/subastas/api/shipments/"
                      + shipment_id
                      + "/track",
                      {
                          "status": "in_transit",
                          "location": "Ruta 1",
                          "description": "en camino",
                      },
                  )["data"]
                  assert tracked["status"] == "in_transit"
                  delivered = _pj(
                      eco.sbs_base
                      + "/subastas/api/shipments/"
                      + shipment_id
                      + "/deliver",
                      {},
                  )["data"]
                  assert delivered["status"] == "delivered"
                  mutual = _pj(
                      eco.sbs_base + "/subastas/api/reputation/mutual",
                      {
                          "order_id": order_id,
                          "buyer_account": "SBS-buyer001",
                          "seller_account": "SBS-seller01",
                          "evidence": "moto entregada y pagada 650",
                      },
                  )["data"]
                  assert mutual["order_id"] == order_id
                  assert "buyer_to_seller_network" in mutual
                  assert "history_seller_recorded" in mutual
              finally:
                  eco.close()


          def test_2_direct_purchase(tmp_path: Path) -> None:
              eco = _Eco(tmp_path)
              try:
                  assert eco.link.register_app()[0] is True
                  eco.link.register_account("Vendedor Dos")
                  eco.link.register_account("Comprador Dos")
                  eco.store.add_account(
                      account_id="SBS-s2", zid=None, name="Vendedor Dos"
                  )
                  eco.store.add_account(
                      account_id="SBS-b2", zid=None, name="Comprador Dos"
                  )
                  _pf(
                      eco.sbs_base + "/subastas/listing",
                      {
                          "seller_account": "SBS-s2",
                          "title": "Bicicleta",
                          "description": "nueva",
                          "base_price": 120,
                      },
                  )
                  listing_id = eco.store.list_open()[0]["listing_id"]
                  order = _pj(
                      eco.sbs_base + "/subastas/api/orders",
                      {
                          "listing_id": listing_id,
                          "buyer_account": "SBS-b2",
                          "source": "direct",
                      },
                  )["data"]
                  assert order["subtotal"] == 120.0
                  assert order["platform_fee"] == 6.0
                  paid = _pj(
                      eco.sbs_base
                      + "/subastas/api/orders/"
                      + order["order_id"]
                      + "/pay",
                      {"provider": "cash"},
                  )["data"]
                  assert paid["status"] == "paid"
                  settled = _pj(
                      eco.sbs_base
                      + "/subastas/api/orders/"
                      + order["order_id"]
                      + "/settle",
                      {},
                  )["data"]
                  assert settled["settlement"]["net_amount"] == 114.0
                  final_order = _gj(
                      eco.sbs_base
                      + "/subastas/api/orders/"
                      + order["order_id"]
                  )["data"]
                  assert final_order["status"] == "settled"
              finally:
                  eco.close()


          def test_3_radar_vip(tmp_path: Path) -> None:
              eco = _Eco(tmp_path)
              try:
                  assert eco.link.register_app()[0] is True
                  scan = _pj(
                      eco.sbs_base + "/subastas/api/radar/scan",
                      {
                          "title": "Moto en EEUU",
                          "category": "vehiculos",
                          "purchase_price": 100,
                          "shipping_cost": 10,
                          "fees": 5,
                          "estimated_sale_price": 200,
                          "demand_score": 0.8,
                          "risk_score": 0.2,
                      },
                  )["data"]
                  assert scan["total_cost"] == 115.0
                  assert scan["expected_profit"] == 85.0
                  assert scan["margin_percent"] == 73.91
                  assert scan["opportunity_score"] == 76.95
                  bad = _pj(
                      eco.sbs_base + "/subastas/api/radar/scan",
                      {
                          "title": "Lote ropa",
                          "category": "ropa",
                          "purchase_price": 300,
                          "shipping_cost": 20,
                          "fees": 10,
                          "estimated_sale_price": 320,
                          "demand_score": 0.4,
                          "risk_score": 0.6,
                      },
                  )["data"]
                  assert bad["expected_profit"] == -10.0
                  opportunities = _gj(
                      eco.sbs_base + "/subastas/api/opportunities"
                  )["data"]["opportunities"]
                  assert len(opportunities) == 2
                  revision = _h(eco.sbs_base + "/subastas/revision")
                  assert "Revision SUBASTAS" in revision
                  assert "oportunidades radar: 2" in revision
              finally:
                  eco.close()


          def test_4_offline_resilience(tmp_path: Path) -> None:
              db = SQLiteAdapter(tmp_path / "sbs.db")
              clock = FrozenClock()
              store = SubastasStore(db, clock)
              dead_client = NetworkClient(
                  "http://127.0.0.1:1",
                  timeout_seconds=1.0,
                  max_retries=0,
              )
              server = serve_subastas(store, dead_client)
              thread = threading.Thread(
                  target=server.serve_forever, daemon=True
              )
              thread.start()
              base = "http://127.0.0.1:" f"{server.bound_port}"
              try:
                  store.add_account(
                      account_id="SBS-off-s", zid=None, name="Vendedor Off"
                  )
                  store.add_account(
                      account_id="SBS-off-b", zid=None, name="Comprador Off"
                  )
                  html = _pf(
                      base + "/subastas/listing",
                      {
                          "seller_account": "SBS-off-s",
                          "title": "Venta offline",
                          "description": "sin red",
                          "base_price": 80,
                      },
                  )
                  assert "SELLADO" in html
                  listing_id = store.list_open()[0]["listing_id"]
                  body = _pj(
                      base + "/subastas/api/bids",
                      {
                          "listing_id": listing_id,
                          "bidder_account": "SBS-off-b",
                          "amount": 90,
                      },
                  )
                  assert body["ok"] is True
                  store.close_listing(
                      listing_id=listing_id, seller_account="SBS-off-s"
                  )
                  order = _pj(
                      base + "/subastas/api/orders",
                      {
                          "listing_id": listing_id,
                          "buyer_account": "SBS-off-b",
                          "source": "auction",
                      },
                  )["data"]
                  assert order["subtotal"] == 90.0
                  paid = _pj(
                      base
                      + "/subastas/api/orders/"
                      + order["order_id"]
                      + "/pay",
                      {"provider": "efectivo"},
                  )["data"]
                  assert paid["status"] == "paid"
                  settled = _pj(
                      base
                      + "/subastas/api/orders/"
                      + order["order_id"]
                      + "/settle",
                      {},
                  )["data"]
                  assert settled["settlement"]["net_amount"] == 85.5
                  summary = _gj(base + "/subastas/api/summary")["summary"]
                  assert summary["listings_total"] == 1
              finally:
                  server.shutdown()
                  server.server_close()
                  thread.join(timeout=5)
                  db.close()
          PYEOF
          echo "commerce e2e written"

      - name: Run SUBASTAS tests (regression plus commerce ADD-ONLY e2e)
        run: |
          set -euo pipefail
          PYTHONPATH="$PWD" python -m pytest -q -p no:cacheprovider --timeout=60 --timeout-method=thread apps/subastas/tests/
          echo "SUBASTAS tests OK"
          find apps/subastas -name '__pycache__' -type d \
            -prune -exec rm -rf {} +

      - name: Commit and push (race-safe)
        run: |
          set -euo pipefail
          git diff --check
          git config user.name "zyra-engines-bot"
          git config user.email "zyra-engines-bot@users.noreply.github.com"
          git pull --rebase --autostash origin "${{ github.ref_name }}" || true
          git add -A
          if git diff --cached --quiet; then
            echo "No changes."
          else
            git commit -m "zyra: Q3 SUBASTAS ADD-ONLY v2 - anchors corrected to the real multi-line route format (count=0 fixed), CommerceStore as new file (original store/link/client/tests untouched), 4 verified insert-patches + serve bind, winner persistence additive, orders/payments/commission/settlement, shipments/tracking/delivery, mutual reputation+history by ZID, Radar VIP; probe + 4 new e2e"
            git push origin "HEAD:${{ github.ref_name }}"
          fi
          echo "=== Q3 SUBASTAS ADD-ONLY COMPLETE ==="
