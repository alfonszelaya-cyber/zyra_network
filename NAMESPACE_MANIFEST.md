# Reserved future namespaces (do not create as packages)
- `shared_engines/identity/registry/` — FUTURE (reserved; shadowed real module `shared_engines/identity/registry.py`, dir removed to unblock imports)
- `shared_engines/tokenization/ledger/` — FUTURE (reserved; shadowed real module `shared_engines/tokenization/ledger.py`, dir removed to unblock imports)
- `shared_engines/verification/signatures/` — FUTURE (reserved; shadowed real module `shared_engines/verification/signatures.py`, dir removed to unblock imports)
