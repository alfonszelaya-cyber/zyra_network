cryptography>=42,<45

# Real face engine (biometrics_face.py)
# Only loaded when ZYRA_BIOMETRICS_PROVIDER=insightface;
# without it the network stays fail-closed.
numpy>=1.24,<2.0
opencv-python-headless>=4.8,<5.0
onnxruntime>=1.16,<2.0
insightface>=0.7,<0.8
